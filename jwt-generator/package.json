{
  "name": "menta-jwt-generator",
  "version": "1.0.0",
  "description": "Helper that creates the jwt token that Github API requires to authenticate a Github App.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Martín Dias",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^8.4.0",
    "yargs": "^12.0.5"
  }
}
